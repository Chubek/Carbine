%struct-type
%swtich=<: =$SWITCH :>

struct m4Builtin { char *name; int id;   };

%%

<:

open(M4_BUILTINS_FILE, "<", "m4-builtins.ls");
my $id = 0;
while (chomp($line = <M4_BUILTINS_FILE>))
{
	print $line, ", ", $id, "\n";
	print $line, ", ", $id++, "\n";
}

close M4_BUILTINS_FILE;

:>
