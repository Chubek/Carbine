%struct-type
%swtich=<: print $SWITCH :>

struct m4Builtin { char *name;  int id;   };

%%

<:
@BUILTINS = qw/changecom changequote decr define defn divert dumpdef errprint eval ifdef ifelse include incr index len m4exit m4wrap mkstemp popdef pushdef shift sinclude substr syscmd traceon traceoff translit undefine undivert exec printf join/;

foreach (@BUILTINS) {
	print $_, ", ", "ID_", uc($_);
}
:>
